# MongoDB connection URI (replace with your own)
MONGODB_URI=mongodb://localhost:27017/medi_aid
# JWT secret for auth
JWT_SECRET=change_this_secret
# Server port
PORT=4000
# Optional: API key for an external chatbot provider
CHATBOT_API_KEY=
